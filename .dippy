allow-read ~/.ucas/**
allow-edit ~/.ucas/**

allow-read ../ucas-tests/**
allow-edit ../ucas-tests/**

allow export UCAS_HOME=*

deny python3 -m ucas * "Ucas is sytem-wide installed use only `ucas ...`"
deny python -m ucas * "Ucas is sytem-wide installed use only `ucas ...`"
deny python3 ucas-bin * "Ucas is sytem-wide installed use only `ucas ...`"
deny python ucas-bin * "Ucas is sytem-wide installed use only `ucas ...`"
deny ./ucas-bin * "Ucas is sytem-wide installed use only `ucas ...`"

allow ucas --dry-run *
allow ucas doctor
allow ucas team *
allow ucas list *
ask ucas team run *

# TESTS
allow python3 -m pytest tests/** *
allow python3 -m pytest tests/**
allow python3 -m pytest tests/ *

# komunikace mcp_agent_mail
allow am-client *

# tmux testing
allow tmux ls *
allow tmux list-windows *
allow tmux list-sessions *
allow tmux capture-pane -p -t ucas*
allow tmux capture-pane -p -t ucas* *
allow tmux send-keys -t ucas-team-pi-mail*
allow tmux send-keys -t ucas-team-pi-mail* *

# integrate other
allow-read /home/michael/work/ai/Dicklesworthstone/mcp_agent_mail/**


allow ./run_tests.sh
