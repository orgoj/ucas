allow-read ~/.ucas/**
allow-edit ~/.ucas/**

allow-read ../ucas-test/**
allow-edit ../ucas-test/**

allow export UCAS_HOME=*

deny python3 -m ucas * "Call application only with `./ucas-bin ...`"
deny python3 ucas-bin * "Call application only with `./ucas-bin ...`"
allow ./ucas-bin --dry-run *
allow /home/michael/work/ai/PROJECTS/ucas/ucas-bin --dry-run *

# komunikace mcp_agent_mail
allow am-client *

# tmux testing
allow tmux ls *
allow tmux list-windows *
allow tmux capture-pane -p -t ucas*
allow tmux capture-pane -p -t ucas* *
allow tmux send-keys -t ucas-team-pi-mail*
allow tmux send-keys -t ucas-team-pi-mail* *

# integrate other
allow-read /home/michael/work/ai/Dicklesworthstone/mcp_agent_mail/**
