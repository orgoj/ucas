# System defaults for UCAS

# Auto-included mods
mods+:
  - run-bash

allowed_acli:
  - acli-claude
  - acli-pi
  - acli-gemini
  - acli-echo

default_acli: acli-echo

# Run configuration
allowed_run:
  - run-bash
  - run-tmux

default_run: run-bash
