# Git Operations

You have access to git tools. Use them to help users with version control tasks.

## Available Git Commands

- `git status` - Check repository status
- `git log` - View commit history
- `git diff` - Show changes
