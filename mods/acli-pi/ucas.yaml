name: acli-pi
description: ACLI for Pi (pi-mono)
acli!:
  executable: pi
  prompt_arg: --prompt
  skills_dir: --skill
  model_flag: --model
  session_arg: --session "{project_root}/.pi/sessions/{uuid}.json"
  ignore_unknown: true
